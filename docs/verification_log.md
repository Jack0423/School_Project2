# 驗證與量測紀錄

這份文件記錄 2026-09-19 到 09-23 之間，模型端做過的每一項檢查、測試與量測，
包含完整數據與重現方式。報告與口頭簡報引用數字時以本文件為準。

測量環境：RTX 5070 Ti / Ryzen 7 5800X / Windows 11 / Python 3.10.11 / torch 2.11.0+cu128。

---

## 一、一頁摘要

| 項目 | 數字 |
|---|---|
| 單元測試 | 162 項，全數通過（期間由 109 增加而來） |
| 美感模型 | SRCC 0.7925 / PLCC 0.8872（AVA 驗證集 1,400 張） |
| 技術模型 | SRCC 0.7951 / PLCC 0.8314（KonIQ 驗證集 2,015 張） |
| 半尺寸解碼 | 整條管線快 4.3 倍，325 張 15 GB 從 2 分鐘降為 26～30 秒 |
| 實際掃描 | 325 張 ARW 全部成功，找出 105 組連拍 |
| 篩選效果 | 每組只留最佳一張時，325 張 → 114 張 |
| 分組準確度 | 人工檢查 140 組，誤分 0 組 |
| 雜訊偵測 | 以 77 張人工盲標樣本校準，門檻 1.43，誤報 0 張、漏報 5 張 |

---

## 二、模型指標（2026-09-19 重新量測）

`python eval_models.py`

| 模型 | 驗證集 | PLCC | SRCC |
|---|---|---|---|
| 美感（`nima_aes_dist.pth`） | AVA 1,400 張 | 0.8872 | 0.7925 |
| 技術（`nima_tech_best.pth`） | KonIQ 2,015 張 | 0.8314 | 0.7951 |

`python compare_aesthetic_models.py`：同一組 1,400 張、統一以 AVA 群眾平均分為對照答案。

| 美感模型版本 | 訓練資料 | SRCC | PLCC | 輸出範圍 |
|---|---|---|---|---|
| 舊版二元 | 3,920 張 | 0.7512 | 0.8411 | −0.328 ～ 1.549 |
| 二元 + 新資料 | 5,600 張 | 0.7542 | 0.8486 | −0.322 ～ 1.482 |
| **分佈 + EMD loss（採用）** | 5,600 張 | **0.7925** | **0.8872** | 2.899 ～ 7.642 |

兩個結論：資料量增加 43% 只帶來 +0.003，換掉標籤形式才是關鍵（+0.041）；
分佈模型的輸出天然落在合理範圍，不需要裁切。

---

## 三、效能量測

### 3.1 半尺寸解碼（`half_size=True`）

先以 8 張 RAW 初測，再用 325 張中的 60 張做正式量測，並**調換執行順序重測一次**，
確認不是作業系統檔案快取造成的差異。

| 量測 | 樣本 | 全尺寸 | 半尺寸 | 倍數 |
|---|---|---|---|---|
| 單純解碼 | 8 張（my_photos） | 807 ms | 204 ms | 4.0× |
| 整條管線（全尺寸先跑） | 60 張 | 373 ms | 80 ms | 4.66× |
| 整條管線（半尺寸先跑） | 60 張 | 366 ms | 85 ms | 4.29× |
| **325 張總時間** | 325 張 15 GB | **2 分 03 秒** | **約 26～30 秒** | **4.3×** |

對評分的影響（120 張）：

| 項目 | 平均差 | 最大差 |
|---|---|---|
| 綜合分 | 0.34 ～ 0.56 | 2.10 |
| 美感分 | 0.39 ～ 0.63 | 3.23 |
| 技術分 | 0.61 ～ 0.77 | 4.75 |

60 張中有 3 張的狀態判定改變，全部是分數本來就壓在門檻上的：
技術分 59.63 → 60.84、60.20 → 58.92、以及一張美感分跨過 62。
該批有 14/60 的技術分落在門檻 ±3 分以內，這種邊緣照片被小幅差異推過去是必然的。

另外量到：RAW 內嵌預覽圖讀取約 1 ms（ARW 為 1616×1080），
用於瀏覽顯示比任何快取機制都快，這也是不做解碼快取的理由之一。

### 3.2 整個資料夾掃描

`python run_batch.py F:\testphoto`，325 張 Sony ARW、共 15 GB：

- 325/325 成功，耗時 2 分 03 秒（每張 378 ms，4 條執行緒解碼、單一執行緒推論）
- 改用半尺寸後約 26～30 秒
- 每張都取得 1280 維特徵

---

## 四、程式碼整合的驗證

### 4.1 併入批次管線與特徵向量（commit 802d827）

| 檢查 | 結果 |
|---|---|
| 直接套用原提案 | 162 項中 1 項失敗——回傳格式合約測試（結果多了一個欄位） |
| 改為選用參數後 | 133 項全過（原 126 項 + 新增 7 項） |
| 回歸比對 | my_photos 17 張（含 8 張 RAW）在併入前後的評分輸出**逐位元組相同** |
| Windows + GPU 實跑 | 17/17 成功，含載入模型共 12.5 秒 |

### 4.2 測試數量的演進

| 日期 | 數量 | 增加的原因 |
|---|---|---|
| 09-19 | 109 | 交接時的狀態 |
| 09-22 | 126 | 效能量測與模型報表（`test_metrics.py`） |
| 09-23 | 133 | 特徵向量、模型版本標籤 |
| 09-23 | 154 | 三種挑選模式、相機識別、小數秒解析 |
| 09-23 | 158 | 半尺寸解碼 |
| 09-23 | 162 | 分組門檻與雜訊門檻的校準 |

---

## 五、組員交付物核對（2026-09-22）

三份交付物都逐行對照程式碼，並用他們附的結果檔驗證。

**宋宇宸（批次管線、相似照片與連拍）**：報告內容與程式碼、結果檔完全一致。
28 張測試照片（8 JPG、10 ARW、10 CR3）全部分析成功，特徵皆為 1280 維，
找出一組 2 張連拍，相似度 0.9005、拍攝時間相差 0.22 秒——與他的敘述相符。

**童小席（前台與資料庫）**：三項敘述皆屬實（分開保存兩個分數、滑桿不重跑模型、
RAW 傳入已解碼影像且解碼參數正確）。發現一個缺陷：選擇資料夾時會清空整個資料庫。

**李奇翰（RAW 與 XMP）**：兩項敘述與程式碼不符——
解碼參數寫的是 `no_auto_bright=False`（與推論端相反），
批次重寫工具接上真正的評分函式會每張失敗（回傳值型別不符）。
另有兩處會破壞資料：預覽程式每開啟一張照片就以寫死的假分數覆寫該張的 `.xmp`；
寫入時整個檔案重寫，會清掉 Lightroom 的編修內容。其星等映射本身沒問題，
套用在宋宇宸 28 張的真實分數上，1～5 星皆有分布（6/4/5/10/3 張）。

---

## 六、分組功能的驗證與校準

### 6.1 325 張的分組結果

| | 相似照片檢索 | 連拍候選 |
|---|---|---|
| 判斷依據 | 只看畫面內容 | 內容 + 拍攝時間 + 相機 |
| 組數 | 96 | 105 |
| 涉及照片 | 281 張 | 277 張 |
| 每組留最佳一張 | 325 → 140 張 | 325 → 153 張 |

相機識別：325 張全部以機身序號認定（Sony ARW 的 `InternalSerialNumber`），無一張無法識別。

組內分數落差（105 組的中位數）：美感 2.06、技術 3.33、綜合 2.40。
其中 19 組的綜合分落差不到 1 分，32 組的技術分落差超過 5 分。

「綜合分挑出的那張」與其他排序方式的一致性：

| 比較對象 | 一致的組數 |
|---|---|
| 技術分最高者 | 78 / 105（74%） |
| 美感分最高者 | 80 / 105（76%） |
| 技術分最高者（只看落差 > 5 分的 32 組） | 26 / 32（81%） |

結論：同一組連拍的構圖幾乎相同，美感分沒有鑑別度，實際是技術分在決定；
但在真正有差別的組裡，兩種排序挑出同一張的比例達 81%，
因此維持使用綜合分，不另外增加排序選項。

### 6.2 人工標註（兩輪）

用網頁進行，照片以 RAW 內嵌預覽圖顯示，標註者逐組判斷。

**第一輪（門檻 0.90 / 2 秒）**

- 105 組全部判定為正確分組，**誤分 0 組**
- 抽查 15 張未被分組的照片，其中 **11 張其實有同伴被漏掉**

漏分原因分解：

| 原因 | 張數 | 實例 |
|---|---|---|
| 相似度未達 0.9 | 4 | 實際為 0.8089 ～ 0.8929 |
| 拍攝時間超過 2 秒 | 3 | 相似度 0.92 ～ 0.93，時間差 3 ～ 4 秒 |
| 卡在「須與組內每一張都達門檻」 | 4 | 與組內三張都超過 0.9，只有一張 0.8887，整組進不去 |

**參數掃描**（以人工標註為答案）：

| 門檻 | 秒數 | 組數 | 救回漏分 | 325 張篩到 | 最大組 |
|---|---|---|---|---|---|
| 0.90 | 2 | 105 | 0 / 11 | 153 | 5 |
| 0.90 | 5 | 101 | 4 / 11 | 145 | 8 |
| 0.87 | 5 | 97 | 6 / 11 | 121 | 10 |
| **0.85** | **5** | **98** | **9 / 11** | **114** | **10** |
| 0.82 | 5 | 95 | 9 / 11 | 109 | 10 |

選 0.85 / 5 秒的理由：救回最多，且再放寬到 0.82 不會再增加。

**第二輪（驗證放寬後有無副作用）**

只標註放寬參數後「有變動的 35 組」，其中 33 張是新加入的照片。
結果：**35 組全部正確，誤分 0 組**。

兩輪合計 140 組人工判斷，誤分 0 組。

---

## 七、雜訊偵測的校準

先前此功能停用，原因是樣本不足、正負樣本重疊。這次以 90 張照片分兩輪盲標校準。

盲標設計：ISO 與程式算出的數值都不顯示，順序打亂，
每張顯示原始解析度的 100% 中央裁切（降尺寸會把雜訊平均掉，看不出來）。

| 輪次 | 照片 | 標註結果 |
|---|---|---|
| 第一輪 | ISO 100～125 共 30 張、ISO 10000 共 30 張 | 30 有雜訊、23 乾淨、7 不確定 |
| 第二輪 | ISO 2000～6400 共 30 張 | 24 有雜訊、1 乾淨、5 不確定 |

**標註品質的佐證**：第一輪的判斷與 ISO 完全一致（53/53）。ISO 在標註時是隱藏的。

**第二輪的發現**：從 ISO 2000 開始就能看出雜訊，比原先假設的 6400 低很多。

扣掉 13 張「不確定」後的 77 張有效樣本：

| 標註 | 張數 | 程式量測值（sigma） |
|---|---|---|
| 有雜訊 | 53 | 1.319 ～ 2.79 |
| 乾淨 | 24 | 0.430 ～ 1.427 |

兩群在 1.32 ～ 1.43 之間重疊，因此沒有任何門檻能同時零誤報與零漏報：

| 門檻 | 誤報（乾淨被標記） | 漏報（有雜訊未標記） |
|---|---|---|
| 0.95 | 3 張 | 0 張 |
| 1.30 | 3 張 | 0 張 |
| **1.43（採用）** | **0 張** | **5 張** |
| 1.90 | 0 張 | 19 張 |

選 1.43 的理由：技術量測是顯示給使用者看的附註，誤報的代價高於漏報——
把乾淨照片標成有雜訊會損害整體可信度；漏掉的 5 張都是 ISO 2000～2500、
量測值 1.32～1.41 的輕微雜訊，使用者通常不會察覺。

**額外驗證**：把同一批 ISO ≤ 800 的 76 張全部計算一次，只有 3 張超過門檻
（1.60～1.65，皆為 ISO 400～800，本身可能就有輕微雜訊），約 4%。

**已知限制**：這個估計量無法完全區分「高頻細節」與「雜訊」。
合成測試影像中，細節豐富那張的量測值是 53.5，比刻意加入雜訊那張的 39.8 還高。
換相機或大量拍攝細碎紋理（樹葉、砂石）時必須重新校準。此限制已寫成測試記錄。

---

## 八、過程中發現並修正的缺陷

| 缺陷 | 影響 | 處理 |
|---|---|---|
| 相機識別只讀 `SerialNumber` | Sony 的 JPG 沒有此欄位、ARW 只有 `InternalSerialNumber`，整個資料夾永遠 0 組且不報錯 | 改為三層判斷，並可用 `--camera-match` 選擇嚴格程度 |
| Python 3.10 無法解析兩位小數秒 | ExifTool 對 Sony 寫的是 `.32`，3.11 以前只接受 3 或 6 位，解析失敗後退回只精確到秒，同一秒內的連拍分不出先後（開發機 3.10、批次管線開發者的 Mac 是 3.12，同一份程式碼結果不同） | 統一補到 6 位再解析 |
| 回傳格式合約被破壞 | 前台依賴回傳欄位，無條件新增欄位會破壞合約並使 `--json` 輸出膨脹 | 改為選用參數 `return_features=True` |
| 資料庫分不出模型版本 | 換模型後舊分數與新分數混在一起排序 | 新增 `model_version()`，含權重指紋與解碼尺寸 |

---

## 九、重現方式

```bash
python -m unittest discover -s tests -t .          # 162 項測試
python eval_models.py                              # 模型指標
python compare_aesthetic_models.py                 # 美感模型版本比較
python run_batch.py <資料夾> --output batch.jsonl   # 批次分析
python run_bursts.py batch.jsonl photo_metadata.json --output bursts.json
```

效能量測與模型成效報表另見 `benchmark_gpu.py` 與 `model_report.py`，
輸出含時間戳記於 `reports/`。
